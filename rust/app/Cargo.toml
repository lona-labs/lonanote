[package]
name = "lonanote_app_lib"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib"]

[build-dependencies]
flutter_rust_bridge_codegen = { workspace = true }
anyhow = { workspace = true, features = ["backtrace"] }

[dependencies]
flutter_rust_bridge = "=2.6.0"
